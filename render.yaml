services:
  - type: web
    name: ai-portfolio-assistant
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn src.api.main:app --host 0.0.0.0 --port $PORT"
    envVars:
      - key: PYTHON_VERSION
        value: "3.11.10"

# v6.4: Optimizer & ML Hardening
